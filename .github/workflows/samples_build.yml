name: Build Samples (reusable)
on:
  workflow_call:

jobs:
  build-samples:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create temp Unity project
        env:
          UNITY_VER: 6000.2.8f1
        run: |
          mkdir -p TempProject/Assets TempProject/Packages TempProject/ProjectSettings
          cat > TempProject/Packages/manifest.json << 'JSON'
          {
            "dependencies": {
              "com.unity.modules.ui": "1.0.0",
              "com.unity.modules.uielements": "1.0.0",
              "com.unity.ugui": "1.0.0",
              "com.unity.textmeshpro": "3.0.9",
              "com.unity.2d.tilemap": "1.1.0",
              "com.unity.2d.sprite": "1.0.0",
              "com.unity.test-framework": "1.4.5",
              "com.caskev.grid-toolkit": "https://github.com/kevincastejon/com.caskev.grid-toolkit.git#${{ github.sha }}"
            }
          }
          JSON
          echo "m_EditorVersion: ${UNITY_VER}" > TempProject/ProjectSettings/ProjectVersion.txt

      - name: Add editor build script
        run: |
          mkdir -p TempProject/Assets/Editor
          cat > TempProject/Assets/Editor/BuildSamples.cs << 'CS'
          using System;
          using System.IO;
          using System.Linq;
          using System.Collections.Generic;
          using System.Reflection;
          using UnityEditor;
          using UnityEditor.PackageManager;
          using UnityEditor.PackageManager.UI;
          using UnityEngine;

          public static class BuildSamples
          {
              [MenuItem("CI/Build WebGL Samples")]
              public static void Run()
              {
                  PlayerSettings.WebGL.compressionFormat = WebGLCompressionFormat.Brotli;
                  PlayerSettings.WebGL.decompressionFallback = true;

                  EnsureTmpEssentials();

                  string pkgName = "com.caskev.grid-toolkit";
                  var list = Client.List(true);
                  while (!list.IsCompleted) System.Threading.Thread.Sleep(50);
                  var pkg = list.Result.FirstOrDefault(p => p.name == pkgName);
                  if (pkg == null) throw new Exception("Package not found: " + pkgName);

                  var samples = (Sample.FindByPackage(pkgName, pkg.version) ?? Enumerable.Empty<Sample>()).ToList();
                  if (!samples.Any()) { Debug.LogWarning("No samples found via API"); return; }

                  var importedDirs = new List<string>();

                  foreach (var s in samples)
                  {
                      try
                      {
                          if (!(bool)(typeof(Sample).GetProperty("isImported")?.GetValue(s) ?? false))
                          {
                              var mi = typeof(Sample).GetMethod("Import", new Type[]{ typeof(bool) }) ??
                                       typeof(Sample).GetMethod("Import", Type.EmptyTypes);
                              if (mi == null) throw new Exception("Sample.Import method not found");
                              if (mi.GetParameters().Length == 1) mi.Invoke(s, new object[]{ false }); else mi.Invoke(s, null);
                          }
                          string path = (string)(typeof(Sample).GetProperty("importPath")?.GetValue(s) ?? string.Empty);
                          if (string.IsNullOrEmpty(path))
                          {
                              var guess = Path.Combine("Assets","Samples",pkgName, MakeSafe(s.displayName ?? "Sample"));
                              if (Directory.Exists(guess)) path = guess;
                          }
                          if (!string.IsNullOrEmpty(path) && Directory.Exists(path)) importedDirs.Add(Norm(path));
                      }
                      catch (Exception ex)
                      {
                          Debug.LogWarning("Sample import skipped: " + (s.displayName ?? "(no name)") + " -> " + ex.Message);
                      }
                  }

                  AssetDatabase.Refresh();
                  if (!WaitForCompilation(TimeSpan.FromMinutes(10)))
                      throw new Exception("Timeout waiting for script compilation");

                  string outRoot = "build-webgl";
                  Directory.CreateDirectory(outRoot);

                  foreach (var dir in importedDirs.Distinct())
                  {
                      string folderName = Path.GetFileName(dir.TrimEnd('/','\\'));
                      var wanted = new HashSet<string>(new[]{ folderName }, StringComparer.OrdinalIgnoreCase);

                      var scenes = Directory.GetFiles(dir, "*.unity", SearchOption.AllDirectories)
                                            .Where(p => wanted.Contains(Path.GetFileNameWithoutExtension(p)))
                                            .Select(Norm)
                                            .ToArray();
                      if (scenes.Length == 0)
                      {
                          scenes = Directory.GetFiles(dir, "*.unity", SearchOption.AllDirectories)
                                            .Select(Norm).ToArray();
                      }
                      if (scenes.Length == 0)
                      {
                          Debug.LogWarning("No scene found under: " + dir);
                          continue;
                      }

                      string safeName = MakeSafe(folderName);
                      string buildDir = Path.Combine(outRoot, safeName);
                      Directory.CreateDirectory(buildDir);

                      var report = BuildPipeline.BuildPlayer(scenes, buildDir, BuildTarget.WebGL, BuildOptions.None);
                      if (report.summary.result != UnityEditor.Build.Reporting.BuildResult.Succeeded)
                          throw new Exception("Build failed for sample: " + safeName);
                  }

                  AssetDatabase.Refresh();
              }

              static void EnsureTmpEssentials()
              {
                  var settings = AssetDatabase.LoadAssetAtPath<ScriptableObject>("Assets/TextMesh Pro/Resources/TMP Settings.asset");
                  if (settings) return;
                  EditorApplication.ExecuteMenuItem("Window/TextMeshPro/Import TMP Essential Resources");
                  AssetDatabase.Refresh();
                  WaitForCompilation(TimeSpan.FromMinutes(5));
              }

              static bool WaitForCompilation(TimeSpan timeout)
              {
                  var start = DateTime.UtcNow;
                  while (EditorApplication.isCompiling)
                  {
                      if (DateTime.UtcNow - start > timeout) return false;
                      System.Threading.Thread.Sleep(200);
                  }
                  return true;
              }

              static string Norm(string p) => p.Replace('\\','/');
              static string MakeSafe(string name)
              {
                  foreach (char c in Path.GetInvalidFileNameChars()) name = name.Replace(c, '_');
                  return name;
              }
          }
          CS






      # - name: Cache Library
      #   uses: actions/cache@v3
      #   with:
      #     path: TempProject/Library
      #     key: Library-GridToolkit-WebGL-6000.2.8f1
      #     restore-keys: |
      #       Library-GridToolkit-WebGL-

      - name: Build WebGL
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          projectPath: TempProject
          targetPlatform: WebGL
          unityVersion: 6000.2.8f1
          buildMethod: BuildSamples.Run
          versioning: None
          customParameters: -stackTraceLogType Full
      - name: Prepare samples artifact
        run: |
          set -e
          mkdir -p public-samples/samples
          cp -r TempProject/build-webgl/* public-samples/samples/ || true

          if [ ! -f public-samples/samples/index.html ]; then
            printf '%s\n' \
              '<!doctype html>' \
              '<meta charset="utf-8">' \
              '<title>Samples</title>' \
              '<h1>Samples</h1>' \
              '<ul id="list"></ul>' \
              '<script>' \
              "fetch('./').then(r=>r.text()).then(t=>{" \
              "  const rx=/href=\"([^\"]+)\\//g;let m,items=[];" \
              "  while((m=rx.exec(t))!==null){const n=m[1];if(!n.startsWith('.')&&n!=='')items.push(n);}" \
              "  const ul=document.getElementById('list');" \
              "  if(items.length===0){ul.innerHTML='<li>No samples</li>';return;}" \
              "  items.forEach(n=>{if(n==='index.html')return;const li=document.createElement('li');const a=document.createElement('a');a.href=n+'/index.html';a.textContent=n;li.appendChild(a);ul.appendChild(li);});" \
              '});' \
              '</script>' \
            > public-samples/samples/index.html
          fi
      - name: Upload samples artifact
        uses: actions/upload-artifact@v4
        with:
          name: samples-site
          path: public-samples
